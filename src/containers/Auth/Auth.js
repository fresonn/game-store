import React from "react"
import classes from "./Auth.module.scss"

const Auth = props => {
    return (
        <div className={classes.auth}>
            <h1>Auth</h1>
        </div>
    )
}

export default Auth